<% if @board.errors.any? %>
  <h2>エラーが発生しました.</h2>
  <% @board.errors.full_messages.each do |e| %>
    <%= e %>
  <% end %>
<% end %>


<%= form_for @board do |f| %>
  <%= f.text_field :name, placeholder: 'ここにタイトル書け' %>
  <%= f.submit %>
<% end %>